info:
  name: Get Product By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/products/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
