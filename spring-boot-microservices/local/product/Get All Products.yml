info:
  name: Get All Products
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/products"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
