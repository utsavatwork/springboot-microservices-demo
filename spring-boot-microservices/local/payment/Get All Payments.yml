info:
  name: Get All Payments
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/payments"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
