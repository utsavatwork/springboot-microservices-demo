info:
  name: Get All Orders
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/orders
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
