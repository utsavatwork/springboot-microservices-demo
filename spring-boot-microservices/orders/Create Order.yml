info:
  name: Create Order
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/orders
  body:
    type: json
    data: |-
      {
        "productId": 1,
        "quantity": 2,
        "customerId": 5089
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
